import SearchBar from "./SearchBar";

const Header = () => {
    return (
        <header className="relative w-full h-20 px-6 border-stone-300 border-b flex justify-center items-center">
            <SearchBar />
        </header>
    );
};

export default Header;
